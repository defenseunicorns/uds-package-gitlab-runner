kind: ZarfPackageConfig
metadata:
  name: gitlab-runner-test
  version: 0.0.1
  description: A test package with a git repo containing a gitlab runner config

components:
  - name: full-repo
    repos:
      # This references a commit that has a .gitlab-ci.yml in it - to update this push a PR and a new commit.
      - https://github.com/defenseunicorns/uds-package-gitlab-runner.git@112875cfd0b05ca7c0486258afa40f5fe80d4bdf
