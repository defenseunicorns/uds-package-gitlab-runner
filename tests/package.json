{
  "name": "glr-tests",
  "description": "Tests for the UDS GitLab Runner package",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.24.1",
    "kubernetes-fluent-client": "3.10.7",
    "vitest": "^4.0.0"
  }
}
