# yaml-language-server: $schema=https://raw.githubusercontent.com/defenseunicorns/zarf/main/zarf.schema.json
kind: ZarfPackageConfig
metadata:
  name: gitlab-runner
  description: "UDS GitLab Runner Zarf Package"
  # x-release-please-start-version
  version: "16.9.1-uds.2"
  # x-release-please-end

variables:
  - name: JOB_RUNNER_NAMESPACE
    default: gitlab-runner-sandbox

components:
  - name: gitlab-runner
    required: true
    description: "Deploy gitlab-runner"
    import:
      path: common
    only:
      flavor: registry1
    charts:
      - name: gitlab-runner
        valuesFiles:
          - values/registry1-values.yaml
    images:
      - "registry1.dso.mil/ironbank/gitlab/gitlab-runner/gitlab-runner:v16.10.0"
      - "registry1.dso.mil/ironbank/gitlab/gitlab-runner/gitlab-runner-helper:v16.10.0"
      - "registry1.dso.mil/ironbank/redhat/ubi/ubi9:9.3"

  - name: gitlab-runner
    required: true
    description: "Deploy gitlab-runner"
    import:
      path: common
    only:
      flavor: upstream
    charts:
      - name: gitlab-runner
        valuesFiles:
          - values/upstream-values.yaml
    images:
      - "registry.gitlab.com/gitlab-org/gitlab-runner:v16.10.0" # renovate: extractVersion=^alpine-(?<version>.*)$
      - "registry1.dso.mil/ironbank/gitlab/gitlab-runner/gitlab-runner-helper:v16.10.0"
      - "library/alpine:20240315"
