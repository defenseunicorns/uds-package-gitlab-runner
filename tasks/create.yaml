tasks:
  - name: runner-package
    actions:
      - description: Create UDS Gitlab Runner Zarf package for Deployment
        cmd: mkdir -p build && cd build && zarf package create ../ --skip-sbom --confirm