variables:
  - name: FLAVOR
    default: "registry1"

tasks:
  - name: runner-package
    actions:
      - description: Create UDS Gitlab Runner Zarf package for Deployment
        cmd: zarf package create --confirm --no-progress --architecture=${ZARF_ARCHITECTURE} --flavor ${FLAVOR}
